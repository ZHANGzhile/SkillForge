# 困难与解决记录

| ID | 问题 / 影响 | 处理方式 | 状态 / 证据 |
|---|---|---|---|
| I001 | PIP_NO_INDEX=1，沙箱内代理连接失败 | 在获准的网络执行环境中安装项目最小依赖，不改变系统持久配置 | 已解决；12 项 pytest 通过 |
| I002 | 未提供 Student endpoint、模型和 GPU 配置 | 已执行真实 smoke test；localhost:8001 返回 WinError 10061，结果保存 results/model_smoke.json；已向用户询问环境 | 待外部配置；正式模型评测和训练尚未开展 |
| I003 | Docker CLI 存在但 Linux 引擎管道不存在 | 提供 Dockerfile/compose，本地 venv + SQLite 继续开发 | 容器运行待 Docker 引擎启动；未声称 compose 验收通过 |
| I004 | 24 任务首次回归为 23/24；全额退款后又检查退款资格，导致误拒绝 | 将已执行动作的 verification/stop 判断前置；保留全额退款回归案例 | 已解决；24/24 工程任务通过 |
| I005 | 管道列表推导式括号错误导致 pytest collection 失败 | 修复列表边界后重新运行全套 | 已解决；21 项测试通过 |
| I006 | 新版 Starlette 测试客户端提示 httpx/BlockingPortal 弃用 | 保留兼容运行，不通过屏蔽 warning 隐藏；记录实际依赖版本 | 非阻塞，上游迁移待后续处理 |
| I007 | UNKNOWN 如果按 false 统计，会误算错误复用率 | UNKNOWN 保持 null，并单独统计 unknown_applicability_attempts；仅明确 INAPPLICABLE 算已知错误复用 | 已修复；脚本 B2 的 2 次未知复用不会冒充已证实的负迁移 |
| I008 | 重复 demo 的随机轨迹来源会与不可变 Registry 同版本冲突 | 每轮 demo 创建独立 UUID 目录，保留历史产物 | 已修复；重复一键演示成功 |
| I009 | 模型候选上下文若包含 validation statistics，可能泄露验证案例 | 输出候选时剔除 statistics 和 source_trajectory_ids；只保留执行所需契约 | 已修复 |
| I010 | 脚本决策轮数被标为 LLM 调用会误导成本分析 | 分离 decision_calls 和 llm_calls，工程脚本 llm_calls=0；新增回归断言 | 已修复；25 项测试通过 |

后续追加具体失败、根因、修复以及回归验证命令。

## 第二轮记录

| ID | 问题 / 影响 | 处理方式 | 状态 / 证据 |
|---|---|---|---|
| I011 | 原 smoke 分区共享模板、固定 O1/C1，不能支持实例和结构隔离实验 | 新增独立实验生成器、动态实体、模板内容哈希、不可变 manifest、test 独占组合；保留 smoke 原用途 | 29 项测试通过，含篡改/跨分区内容复制拦截 |
| I012 | 工具读取超时耗尽后，脚本策略先检查缺失状态而再次读取 | 将已耗尽错误出口放在缺失状态查询前 | retry_exhausted 实验任务正确转人工 |
| I013 | 旧 Compiler 会把暂时性故障当作边界证据，且失败来源引用过宽 | 按实际拒绝类型和违反的条件筛选反例，新增取消订单编译及顺序证据校验 | 已解决；33 项测试通过 |
| I014 | 首轮隔离对照 B3 为 75/78：Gate 吞掉读取耗尽错误，随后重新查询 | 显式 gate_observations 传给模型，任务内记住耗尽调用，禁止换 step/key 重置预算；保留失败目录 75207fa2-071d-4cd1-b05a-7bf29544dc74 | 已修复；36 项测试通过，五组各3次重复全部通过 |
| I015 | 不同模型 step/Skill fallback 的相同退款使用不同幂等键 | 同任务同 mutation payload 使用同一 operation key；新增跨 step 重复退款测试 | 已修复；数据库只有一笔退款，两个 step 的 key 相同 |
| I016 | 若 B2 复用通过 validation 的 Skill 集合，会暗中受到 B3 筛选影响 | 从成功轨迹独立构造 Naive Skill，先于 validation 保存；B3 仅接纳主方法验证通过集合 | 对照测试检查 Naive 条件无 policy/failure 来源 |
| I017 | 2026-09-09 再次 Student smoke 仍连接拒绝 | 保留 results/round2-model-smoke/model_smoke.json；完成不依赖模型服务的隔离实验与工程回归 | 真实模型 endpoint/模型名称/GPU 配置仍待提供 |

## 第三轮记录

| ID | 问题 / 影响 | 处理方式 | 状态 / 证据 |
|---|---|---|---|
| I018 | 退款后置条件需要初始已退款金额，若读取更新后状态再计算会重复相加 | mutation 前冻结 expected total；通过固定领域 predicate 计算，不添加通用算术 DSL | 已通过带先前部分退款的测试 |
| I019 | 修订历史列表括号错误导致 pytest 收集失败 | 修复语法后重新运行退款定向测试 | 已修复，首批4项通过 |
| I020 | Skill 内重试耗尽位于 result.error，脚本只检查顶层 error 可能再次执行 | 识别 Skill 结果中的耗尽错误并转人工；增加无退款写入及调用数断言 | 已通过，退款工具调用恰好2次、退款记录为0 |
| I021 | 三类完整任务通过，但退款 Decision-level 为0/15；脚本把物流观察错误地设为退款前提 | 退款只要求订单、客户及支付观察；增加固定候选探针回归；保留 eb4bffdf-3be8-4cad-bb88-f87f3f1927a8 失败探针结果 | 已修复；退款15/15，全套43项通过 |

## 本地 Student 部署

| ID | 问题 / 影响 | 处理方式 | 状态 |
|---|---|---|---|
| I022 | 官方运行包约1.47GB，单连接下载慢 | 官方源分段并行下载，复用已下载部分，整体按官方SHA-256核验 | 已解决，完整SHA-256一致 |
| I023 | GPU运行库与模型权重串行下载会增加等待时间 | 从同一官方ZIP提取CLI，先启动仅用于拉取权重的临时服务；完整ZIP核验后切换正式运行库 | 已解决；最终服务37/37层GPU卸载 |
| I024 | Ollama 0.34聊天模板和思考解析不兼容：JSON被归入思考字段，512/2048预算耗尽仍为空Action；自定义Go模板后实际show可退回内嵌Jinja模板 | 不再假定模板修改生效；移除模板覆盖，增加显式Qwen3 ChatML raw适配，预填空思考结束标记，严格验证最终Action；保留所有失败运行 | 集成已解决；accepted smoke通过，5案例无格式错误；通用chat路径未声称修复 |
| I025 | 真实模型5案例仅1个结局合格：提前stop、漏验证、违规尝试、误转人工 | 保留原始轨迹与独立违规计数；下一阶段只在train改进决策提示与采集策略，再开展冻结对照 | 业务能力待改进；实际违规0，详见LOCAL_STUDENT_REPORT |
| I026 | mutation成功后仍重复写入；验证后继续读取直至步数耗尽 | 保留v2/v3提示对照，强调先检查历史；幂等保护有效但不计作模型决策正确 | v2五例2/5，v3五例0/5；后者不激活 |
| I027 | 公开policy的CONFIRMED/NOT_STARTED未标明字段，模型把物流状态规则套用于退款 | 使用order.status/shipment.status完整字段并声明退款不依赖物流；不改变底层eligibility | 全69个train已完成，40个结局合格；并非该改动的独立因果评测 |
| I028 | 同名模型的不同提示实验难以追溯 | ModelClient保存提示版本、原文与SHA-256，轨迹记录model_settings；旧v1保持可选择 | 46项测试通过，包含未知提示版本拒绝和哈希校验 |
| I029 | 单轮69条来源缺取消的第3条执行证据、退款的业务失败反例 | 加入此前完整v2五例真实train轨迹，保留来源哈希及全记录；审计为74条/69任务，无脚本或其他分区数据 | 三类达到门槛并通过validation冻结；没有降低编译验收条件 |
| I030 | 真实五组已执行完，但汇总未交付，进度仍显示运行中；汇总脚本存在括号SyntaxError | 9月15日核对落盘文件，修复报告脚本并实际运行；校验每组78个唯一同集任务，更新最终状态 | 已解决汇总交付，保留原始评测结果 |
| I031 | B2/B3/去Gate组实际Skill调用均为0，固定候选决策0/48（47个tool、1个错误refuse） | 明确标记核心Skill使用环节未达标；不把Gate提供的观察或契约文本效果解释为执行复用收益。下一阶段只在train/validation完善调用协议和选择行为 | 未解决模型行为；正式训练仍未启动，当前test不得用于提示调优 |
| I032 | 原自由动作接口不选择Skill，无法提供可操作演示 | 新增受限动作目录及结构化选择，模型实际选择后绑定参数；保留拒绝/转人工/读取选择，不允许脚本替换答案 | 新演示train9/9、validation27/27，正常案例各调用一次Skill；旧自由接口问题不宣称修复 |
| I033 | 初始演示未执行就stop，或成功后重复Skill | stop要求Executor成功验证回执，已成功Skill不再次列出；这是明确的协议约束，报告中披露 | 新演示通过；未作为模型独立规划能力计分 |
| I034 | 高风险回答理由要求转人工，choice却为refuse | 明确两个动作含义，要求先输出简短业务理由再选目录ID；不根据理由在代码中覆盖choice | train三场景及后续27例通过，失败轨迹保留 |
| I035 | 浏览器测试误读上一个任务的成功状态 | 先等待job URL变化，再等待新任务结束并核对预期标题；下载轨迹也检查Skill次数 | 最终浏览器正常/拒绝/转人工均实测通过 |
| I036 | 旧 BackgroundTasks 任务中断后永久 pending，且缺少逐步持久化轨迹 | 新正式 API 使用 SQLite 队列及单进程文件锁；重启将 running 标记 interrupted，逐工具事件与独立业务数据库保留；重试生成新 ID 和环境 | 新恢复、取消、重试测试通过 |
| I037 | Windows 锁住文件首字节后，第二工作进程读取该字节直接 PermissionError | 初始化时读取文件长度而不读取被锁区域，显式捕获获取锁失败；锁生命周期覆盖整个工作线程 | 多 worker 排他测试通过 |
| I038 | 取消可能在模型返回后仍执行写工具，或把已提交退款当作回滚 | 模型返回后与每次工具调用前检查取消；提交后的业务状态和事件保留，结局为 cancelled，不虚报成功 | 取消前零写入、提交后恰好一笔退款测试通过 |
| I039 | 手工 Task 可以填写已有 dataset_id，可能污染训练来源 | 正式入口强制 manual-unregistered 与内容绑定任务 ID，来源单独记录；原 manifest audit 继续拒绝非冻结 train 来源 | API 隔离测试通过 |
| I040 | 新事件消费测试把审计字段误写为 tool，造成回调异常 | 统一读取原有 tool_name 字段，修复测试和网页时间线，不更改历史审计格式 | 定向测试通过 |
| I041 | 服务升级请求被自动审批服务拒绝，原因是当时账户用量限制；不是代码故障 | 当次不绕过限制，继续可完成工作；9月16日用户要求继续后只读确认进程归属，再经审批重启 | 已恢复；正式工作台上线，重启保留13条记录 |
| I042 | 工作台选择新任务后短暂显示上一任务完成状态，浏览器验收过早读取运行中任务 | selectRun立即重置状态、取消 / 重试按钮和下载链接；旧请求结果按run ID丢弃 | 修复后真实6例及刷新恢复验收通过 |
| I043 | Windows文本写入将LF改为CRLF，导出语料记录的哈希与文件字节不符 | 改为UTF-8字节写入，测试核对实际文件SHA-256；v1失败目录保留，正式语料生成至新v2目录 | 65项测试通过，free-action-v2为有效版本 |
| I044 | 既有自由Action真实语料没有Skill动作目标，直接SFT不足以教授Skill选择 | 导出覆盖审计，明确ready_for_skill_learning=false；不把菜单协议轨迹暗中混入自由Action同上下文证据 | 待补监督与反事实偏好数据；没有伪称训练完成 |
| I045 | 新venv自带旧pip，依赖名称归一化失败后误走源码包，CUDA索引缺flit_core | 在独立训练venv升级pip；固定官方CUDA wheel与PyPI依赖，不改动推理venv | pip26.2.1就绪，固定版本发行包预检通过 |
| I046 | 多GB权重单连接下载长期停在零字节，中断导致执行会话失效 | 先确认Range返回，改为有界并发8MiB分片、精确Content-Range检查、最终官方SHA-256；保留完整分片恢复，后台PID/日志落盘 | 下载已有持续进度，尚未到齐 |
| I047 | 为补Skill监督而混用脚本/受限目录样本，可能被误报为真实自由Action轨迹 | 单独生成deterministic_contract_teacher与executed_contract_counterfactual；标记llm_calls=0，保留实际分支执行、Expected Outcome验收及来源；原真实语料保留原标识 | 186教师样本含33Skill目标；90DPO偏好；合并去重355SFT，未冒充真实模型成绩 |
| I048 | Qwen词表较大，长上下文全量logits投影可能占用大量显存 | 训练保留完整上下文，只计算completion对应位置的logits；增加与完整causal loss/梯度一致性测试 | 代码与无torch的标签边界测试通过；torch数学与GPU测试待依赖到齐 |
| I049 | 长评测中断会迫使整组任务重跑 | 新HF评测逐任务/逐决策探针原子落盘，显式resume核对模型、数据、代码身份 | 已实现，完整GPU评测尚未启动 |
| I050 | 可选HF Xet客户端下载加速安装被自动审批服务拒绝：账户额度限制 | 不执行被拒动作，不改道安装；现有已授权分段下载、bootstrap与流水线继续运行 | 可选加速未启用；不影响当前分段下载代码路径 |
| I051 | 单步训练smoke使用5%预热会被取整为1个预热步，唯一更新步学习率为0 | smoke设置warmup_ratio=0；正式SFT/DPO仍为0.05，训练后继续校验实际权重变化 | 已修复；完整GPU单步检查待模型下载完成 |
| I052 | 已完成评测仅靠evaluation.json存在就跳过，不能发现中断前后模型/数据变化或单任务文件被修改 | 完成评测也调用恢复审核；任务与决策文件绑定执行身份、内容哈希、任务ID/指纹、Skill候选；重建汇总无需重新加载GPU | 两项恢复/篡改回归通过，完整72 passed、1 skipped |
| I053 | HTTP读取超时只限制相邻数据包，极慢持续传输可令最后分片停滞很久 | 新下载器按实际流块检查180秒总耗时并有界重试，已完成分片复用、最终SHA-256不变 | 两项下载回归通过；当前PyTorch旧进程在重启前自行完成并校验通过，新限制供后续下载/恢复使用 |
| I054 | Runtime会将CUDA故障记作普通任务失败，若直接循环可能污染整组模型能力评测 | HF客户端记录基础设施异常，评测保存partial trajectory/decision后停止，不生成该任务的成功检查点；JSON动作格式错误仍按真实模型失败评分 | 故障注入测试通过，模型资源被释放；完整GPU路径待安装完成 |
| I055 | 训练与工作台推理会争用同一张12GB GPU；服务同名时也可能在排队后换了适配器 | 根据活跃流水线心跳预留GPU；私有服务返回实际模型身份指纹，提交/执行及每个生成响应均校验版本一致 | 79项完整回归通过；真实网页验证训练期间禁用提交、历史可读 |
| I056 | 工作台测试隐式使用本机data目录，新检出源码时CI会缺数据；compose也未挂载冻结数据 | 测试自行创建临时隔离数据；新增排除所有本机结果/配置的源码副本回归；compose加入只读data挂载和显式Ollama协议配置 | 干净源码副本79 passed、1 skipped；Docker未实际验收 |
| I057 | Windows GBK控制台无法打印子进程日志中的Unicode替代字符，测试通过后汇总脚本仍退出异常 | 干净源码验收脚本对子进程和自身输出统一UTF-8 | 重新运行包装流程exit0，测试与报告均完成 |
| I058 | 原Windows训练smoke峰值15.39GB、单更新约1098秒；当前wheel未编译Flash Attention，默认GQA路径造成高显存开销 | 实测并固定cuDNN SDPA backend；完整4366-token前后向5.32秒、峰值6.55GB、缓存生成通过；小规模输出和梯度对照通过 | 新main-v2在不删样本/不减轮数的条件下运行；旧结果保留 |
| I059 | 代码与注意力实现变化后，直接沿用原run.json会破坏恢复身份约束 | 新建main-v2，显式审核main-v1 checkpoint-10并记录父run、全部文件哈希；原optimizer/scheduler/RNG一起恢复，禁止改数据/轮数/LR后冒充同一次训练 | 迁移边界回归通过，新流水线开始运行；原未保存11–17步重算 |
| I060 | 旧总违规尝试统计把自动Gate权限读取失败与模型动作混合 | 新轨迹记录decision_origin与policy_attempts，分开模型选择、被拦截Skill及自动Gate；旧verifier口径保留 | 归因回归通过；无新归因证据的旧数据返回null，不伪造回填 |
| I061 | 训练loss不能替代held-out验证loss；按整轨迹违规尝试丢弃会误删正确拒绝 | 单独冻结69个validation任务的141个动作目标，仅做no_grad打分；按动作保留正确目标，不进入optimizer语料 | 来源与隔离检查通过，实际GPU打分待评测完成 |
| I062 | SFT/DPO完成后，Base-validation子进程报No module named scripts | 文件启动将scripts目录而非项目根放入导入路径；统一以python -m scripts.*启动，新增无PYTHONPATH的真实子进程入口检查 | 9项相关回归通过，已恢复流水线；无需重训 |
| I063 | Base验证已完成系统/决策，但likelihood至131/141时progress.tmp替换progress.json报Windows PermissionError | Windows读者短暂占用文件；协调器元数据写入加入有限重试。评测子进程仅对精确的WinError5进度文件冲突最多重启两次，resume重新审核已保存记录；CUDA及其他错误不自动重试 | 入口与恢复8项检查通过；不改已冻结trainer/evaluator源码身份，22:24从检查点恢复 |
| I064 | 9月17日16:55附近恢复协调器被自动审批服务因用量限制拒绝执行 | 不绕过审批；用户22:23再次要求继续后核对原PID退出，重新通过审批启动三个隐藏协调器 | 22:24恢复成功，旧失败记录保留 |
| I065 | SFT已学会部分Skill调用，但PROCESSING物流边界和部分退款任务重复读取至16步耗尽；正常退款及超额退款都出现该问题 | 保留原任务与完整失败轨迹；在同协议验证和test中比较DPO，不缩短预算、不替换模型动作。示例task-156e29963ccd7fb589c8请求退1646/已付15486，却重复读取并未执行退款；不能仅归因为困难边界 | 能力问题尚未解决；22:50已保存53例中46例合格、7例失败，这只是中途观测，不外推最终成绩。是否改善以完整双评测为准 |
| I066 | 完整研究评测后，实际HF DPO工作台HTTP验收仅4/6；普通退款缺写后验证，自定义部分退款16步耗尽 | 验收保留失败并停止后续浏览器签收与最终打包；不将业务执行标记completed直接当作EOC合格，不因test上SFT更高临时换掉validation选定DPO | 2026-09-19核验，尚未解决；证据results/workbench-acceptance/trained-dpo-20260917-223405/summary.json。后续修复必须保留冻结main-v2研究结果并单独标记新版本 |
| I067 | 旧SFT/固定候选DPO没有直接覆盖充分的冗余历史恢复和写后分心再验证；已有失败不能通过修改verifier掩盖 | main-v3只在原train任务执行恢复课程，排除注入读取目标；保留自由Action协议和原工具验收，1,035条执行通过后合并成1,267个SFT样本 | 9月25日来源审核及2项定向测试通过，正式GPU smoke/训练启动；模型效果待完整验证 |
| I068 | 原test成绩已用于分析，再调模型后不能将其当作新的独立test | 训练前冻结seed=20260925的新实例集；原test仅作回归，新实例test不参与数据构造或模型选择，并明确生成器/结构族仍已知 | 新manifest冻结完成，后续新模型评测待训练和validation完成 |
| I069 | Windows PowerShell 5的Set-Content -Encoding UTF8给部署JSON加入BOM，新报告卡片读取它时整个reports接口500 | 部署脚本显式使用无BOM UTF8Encoding；规范化现有部署状态编码，字段值不变 | 真实Chrome桌面/手机报告和进度检查通过，未改冻结核心源码 |
| I070 | 只重新运行verifier不足以判断验收证据完整；删除全部tool_audit后可能仍与已存成功结论一致 | 交付审核从任务fixture重建初态，核对task hash，逐事件核对before/after/diff连续性与最终状态，然后执行EOC审核 | 真实持久HTTP记录的临时测试夹具通过；错误adapter及删除退款审计均被拒绝，不计作真实模型成绩 |

| I071 | 最后回归中cancel返回后的running断言偶发看到cancelled，worker在数据库提交后已合法完成取消 | 用allow_finish事件将运行中取消标记检查与worker最终提交分开，finally始终释放事件；不修改JobStore/Worker语义 | 四项队列测试通过，随后重做独立源码回归；HTTP与浏览器成功记录重新审核后复用 |
